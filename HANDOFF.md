# 인수인계 — 부산도서관 축제 부스 안내 페이지

다른 계정의 Claude가 이어서 작업할 수 있도록 지금까지의 작업 내용, 결정 사항, 미확정 데이터를 정리한 문서입니다.

## 프로젝트 개요

부산도서관 「제4회 창의융합 보드게임 대축제」 안내용 모바일 웹페이지. QR코드로 접속해서 1층 배치도에서 부스를 터치하면 체험 내용이 뜨는 **인터랙티브 지도**입니다. 예약 기능은 없음 — "무엇을 어디서 하는지 3~5초 안에 파악"이 유일한 목적. (기존에 있던 예약 웹앱 `boardgame-festival`과는 역할이 다른 별도 프로젝트입니다.)

## 현재 상태 (완료됨)

- **로컬 경로**: `C:\Users\USER\Desktop\웹앱\boothmap\index.html` — 단일 파일, 의존성 없음(폰트만 CDN), 정적 호스팅 가능
- **배포 URL**: https://logrinssam.github.io/boothmap/
- **GitHub 저장소**: `logrinssam/boothmap` (public, main 브랜치, GitHub Pages 활성화 완료, 루트 기준)
- **GitHub 계정 구조**: `logrinssam`은 **개인 계정이 아니라 GitHub organization**. 실제 로그인 계정은 `yerin-benny`이며 `logrinssam` 조직의 admin 멤버. 새 작업 세션에서 push하려면 `yerin-benny` 계정으로 `gh auth login` 하면 됨 (조직 소속이라 별도 인증 불필요).
- 딥링크 지원: `index.html#booth-9` 형태로 접속하면 해당 부스 시트가 바로 열림 (부스별 QR 제작 시 활용 가능)

## 기술 구조

- **지도**: 배경 이미지가 아니라 SVG로 완전히 재작도. `<g id="rooms">`(공간), `<g id="facilities">`(현수막/출입구 등), `<g id="boothLayer">`(부스, JS로 동적 생성)
- **데이터-좌표 분리** (index.html 내부, 각각 별도 상수):
  - `BOOTHS` 배열 — 부스 콘텐츠(번호/카테고리/제목/대상/설명/위치). **부스명·설명 수정은 여기만 고치면 됨**
  - `BOOTH_GEOM` 객체 — 지도 위 좌표(x,y,w,h 또는 회전 부스는 cx,cy,w,h,rot). 위치 수정은 여기만 고치면 됨
- 필터(전체/보드게임/창의융합), Bottom Sheet 상세정보, 핀치줌/팬/버튼줌, 키보드 접근성, `prefers-reduced-motion` 대응 포함

## 참고 자료 원본 (로컬에만 있음, 저장소에는 미포함)

`C:\Users\USER\Desktop\웹앱\boothmap\` 폴더에 있는 원본 이미지들:

| 파일 | 역할 |
|---|---|
| `전체 부스.png` | **위치 판단의 유일한 정본.** 실제 배치 구조와 부스 상대 위치 기준 |
| `창의융합 배치도.png` | 부스 8~14 확대 버전. ⚠️ 좌측 세로줄 부스 개수가 4개로 보이는 등 정본과 미세하게 달라 보일 수 있음 — 위치는 항상 `전체 부스.png` 우선 |
| `창의융합부스(8~14).png` | 부스 8~14 정식 명칭 목록(표) |
| `부산도서관조감도예시1~4.png` | 부산도서관 공식 조감도 스타일 참고용 (디자인 톤 참고, 데이터 아님) |

원 예약 웹앱 스크린샷(부스 1~3 명칭·대상 확인용)은 이번 세션 초반 사용자가 채팅에 직접 첨부한 이미지였고 로컬 파일로는 없음 — 필요시 사용자에게 재요청.

## 확정된 데이터 (그대로 사용 중)

- 부스 1~3: 예약 웹앱 캡처 기준 명칭·대상 확정
- 부스 8~14: `창의융합부스(8~14).png` 표 기준 명칭 확정
- 대각선 배치 순서(우측, 꿈뜨락 어린이실 벽면 앞): 위→아래로 **부스12 → 13 → 10** (`전체 부스.png` 확대 확인 완료)
- 하단 열 구성: 본부 / **부스14**(넥슨재단) / 미래엔(번호 없는 협력 부스, 클릭 불가 처리) / **부스11**
- 부스12 정식명은 "출동! 카미봇 **지게차** 미션" (구 프로토타입의 "지켜라 미션"은 표와 달라 오기로 판단, 수정함)

## 2026-08-13 업데이트 — 최종 데이터 반영 완료

공식 홍보 배치도(파란색 "1층 부스배치도")와 부스명이 기입된 갱신판 현장 배치도를 반영:

- 부스 6 확정: "1대1 대결 더 지니어스" (예약: 포메이션 / 자유체험: 러시아워), 대상 초등 4학년 이상
- 부스 7 확정: "움직임을 지배하라!" (그래비트랙스, 클러스터, 클라스크), 학년 무관
- 부스 1~7 예약/자유체험 게임명, 부스 8~13 설명 전부 입력 (부스 2 게임은 "미니 우봉고" = Ubongo Mini, 웹 검색으로 교차확인)
- **위치 변경**: 윗줄 왼→오 = 부스 4·3·2·1 (유치부A가 어린이실 쪽 끝), 왼쪽 세로줄 위→아래 = 부스 7·5·6. 구 `전체 부스.png`(범용 라벨)보다 부스명 기입판 배치도가 최신 정본
- 전동놀이 → **전통놀이** 오타 수정, 본부에 "뽑기 교환처" 표기, 하단 안내문에 B1F 혜윰마당 대회장·스탬프 안내 추가

### 2026-08-13 2차 — 부스명 최종 확정 (`정확한 부스명.xlsx`)

사용자가 보낸 엑셀(`C:\Users\USER\Desktop\정확한 부스명.xlsx`)이 부스명의 최종 정본. 부스 2~7 명칭을 엑셀 그대로 교체 (자율체험 태그, "유레카!", "힘의 법칙: 움직임을 지배하라!" 등). 부스 8~13 설명은 엑셀과 기존 입력이 일치함을 확인.

### 2026-08-13 3차 — 부스 8~14는 대상 학년이 원래 없음 (사용자 확인)

창의융합 부스(8~14)는 대상 학년 구분이 없는 것으로 확정. `target:""`로 비우고, target이 빈 부스는 목록 칩과 Bottom Sheet에서 대상 줄 자체가 렌더링되지 않게 처리함.

## 남은 미확정 데이터

- **부스 14(넥슨재단 슬라이딩 수학도둑) 설명**: 미입력 (엑셀에도 없음)

**중요 원칙**: 확인 안 된 부스명·설명·대상을 절대 임의로 만들어내지 말 것. 데이터가 확정되기 전까지는 placeholder를 유지.

## 향후 계획 (사용자가 언급한 것)

- 기존 예약 웹앱(`logrinssam/boardgame-festival`, Firebase 기반 모노레포)과 나중에 연결 가능하도록 구조를 열어둠. 연결 시 `BOOTHS` 각 항목에 `reserveUrl` 필드 추가 + Bottom Sheet에 버튼 하나만 넣으면 됨
- 부스별 QR: `https://logrinssam.github.io/boothmap/#booth-3` 형태로 바로 활용 가능

## 이어서 작업할 때 체크리스트

1. `git clone https://github.com/logrinssam/boothmap.git` (또는 로컬 경로 그대로 사용)
2. `index.html`의 `BOOTHS` 배열에서 미확정 항목(위 목록) 채우기 — 사용자에게 확정 자료 요청
3. 수정 후 `git add index.html && git commit && git push` → GitHub Pages 자동 반영 (수 분 소요)
4. push 권한: `yerin-benny` 계정으로 `gh auth login` (logrinssam은 별도 로그인 계정이 아니라 조직명)
