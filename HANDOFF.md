# 인수인계 — 부산도서관 축제 부스 안내 페이지

다음 세션의 Claude(또는 다른 작업자)가 맥락 없이 바로 이어받을 수 있도록 **현재 상태**를 정리한 문서입니다.
아래 "확정된 데이터"는 전부 실제 반영이 끝난 내용이고, 바꿔야 할 것은 "남은 작업"에만 있습니다.

**최종 갱신: 2026-08-13** · 배포 완료 상태 (마지막 커밋 이후 미배포 변경 없음)

---

## 1. 프로젝트 개요

부산도서관 「제4회 창의융합 보드게임 대축제」 안내용 모바일 웹페이지.
QR로 접속해 1층 배치도에서 부스를 터치하면 체험 내용이 뜨는 **인터랙티브 지도**입니다.

- 예약 기능은 **없음**. "무엇을 어디서 하는지 3~5초 안에 파악"이 유일한 목적
- 별도 프로젝트인 예약 웹앱(`logrinssam/boardgame-festival`, Firebase 모노레포)과는 역할이 다름

## 2. 배포 정보

| 항목 | 값 |
|---|---|
| 로컬 경로 | `C:\Users\USER\Desktop\웹앱\boothmap\` |
| 배포 URL | https://logrinssam.github.io/boothmap/ |
| 저장소 | `logrinssam/boothmap` (public, `main`, GitHub Pages 루트 기준) |
| push 계정 | **`yerin-benny`** 로 `gh auth login`. `logrinssam`은 로그인 계정이 아니라 **organization**이며 `yerin-benny`가 admin 멤버 |
| 반영 시간 | push 후 GitHub Pages 자동 배포까지 수 분 소요 |

딥링크 지원: `#booth-9` 형태로 접속하면 해당 부스 시트가 바로 열림 → **부스별 QR 제작에 그대로 사용 가능**
(예: `https://logrinssam.github.io/boothmap/#booth-3`)

## 3. 폴더 구조

```
boothmap/
├── index.html      ← 결과물 전체. 단일 파일, 의존성 없음(폰트만 CDN)
├── HANDOFF.md      ← 이 문서
├── .gitignore      ← 자료/ 폴더를 저장소에서 제외
└── 자료/           ← 원본 참고 자료 (로컬 전용, git에 올리지 않음)
```

### 자료/ 폴더 안의 원본들

| 파일 | 역할 | 신뢰도 |
|---|---|---|
| `정확한 부스명.xlsx` | **부스명의 최종 정본.** 사용자가 직접 보낸 확정본 | ★ 최우선 |
| `창의융합부스(8~14).png` | 부스 8~14 명칭 표 | 유효 (엑셀과 일치 확인) |
| `전체 부스.png` | 초기 배치도. 부스명 없이 "보드게임/창의융합" 범용 라벨만 있는 **구버전** | ⚠️ 위치는 구버전 — 아래 주의 참고 |
| `창의융합 배치도.png` | 부스 8~14 확대 컷 | 참고용 |
| `부산도서관조감도예시1~4.png` | 부산도서관 공식 조감도 (디자인 톤 참고용, 데이터 아님) | 데이터 아님 |

⚠️ **로컬에 없는 자료 2개** — 2026-08-13에 사용자가 채팅에 첨부만 하고 파일로는 저장되지 않았습니다.
현재 지도의 **위치 배열은 이 두 자료 기준**이라 `전체 부스.png`와는 다릅니다. 필요하면 사용자에게 재요청하세요.

1. **베이지색 갱신판 배치도** — 각 부스 칸에 실제 부스명(유치부A, 1-2학년B 등)이 적힌 최신 평면도. **위치의 최종 정본**
2. **파란색 "1층 부스배치도" 공식 홍보물** — 부스별 예약/자유체험 게임명과 창의융합 설명이 실린 전단

## 4. 기술 구조

`index.html` 하나에 전부 들어있고, 데이터와 좌표가 분리돼 있어 수정 지점이 명확합니다.

- **지도는 배경 이미지가 아니라 SVG로 재작도**했습니다
  - `<g id="rooms">` 공간(광장·어린이실·중앙홀 등) / `<g id="facilities">` 현수막·출입구·본부 등 / `<g id="boothLayer">` 부스(JS로 동적 생성)
- **`BOOTHS` 배열** — 부스 콘텐츠. **명칭·설명·대상 수정은 여기만** 고치면 지도와 목록에 동시 반영
- **`BOOTH_GEOM` 객체** — 지도 좌표. `{x,y,w,h}`, 대각선 부스는 `{cx,cy,w,h,rot}`. **위치 수정은 여기만**
- 부가 기능: 카테고리 필터, Bottom Sheet 상세, 핀치줌/팬/버튼줌, 키보드 접근성, `prefers-reduced-motion` 대응
- **예약 CTA** — 지도 카드 바로 아래 `<a class="cta">`. 예약 웹앱(`https://logrinssam.github.io/boardgame-festival/`)으로 새 탭 이동. 문구·링크 수정은 이 태그 하나만 고치면 됩니다

### BOOTHS 항목 필드

| 필드 | 설명 |
|---|---|
| `title` / `shortTitle` | 정식 명칭 / 목록 칩용 짧은 이름 |
| `target` | 대상 학년. **빈 문자열이면 UI에서 대상 줄 자체가 사라짐** (창의융합 부스가 이 방식) |
| `reserve` / `free` | 예약 보드게임 / 자유체험 게임명. 각각 별도 줄로 렌더링되며, 없으면 그 줄 생략 |
| `description` | 문단형 설명 (창의융합 부스용). `reserve`/`free`가 있으면 그쪽이 우선 |
| `location` | 시트 하단 위치 안내 텍스트 |
| `mapLabel` | 값이 있으면 그 부스는 **협력사 칸(`.partner`)** 으로 그려집니다 — 카테고리 색·번호 알약 대신 미래엔·본부와 같은 베이지 칸에 이 이름이 들어감. 목록 칩에는 "지도에서는 '넥슨' 칸" 안내가 자동으로 붙고, 지도 칸 `aria-label`에도 이름이 포함됩니다. 현재 부스 14 = `넥슨` |

## 5. 확정된 데이터 (전부 반영 완료)

- **부스명 1~14**: `정확한 부스명.xlsx` 그대로. 자율체험 태그, "유레카!", "힘의 법칙: 움직임을 지배하라!" 등 표기까지 일치
- **부스 1~7**: 대상 학년 + 예약/자유체험 게임명 입력 완료
- **부스 8~14**: 대상 학년 **구분이 원래 없음**(사용자 확인). `target:""`로 두어 대상 줄이 렌더링되지 않음
- **부스 8~13**: 창의융합 체험 설명 입력 완료
- **위치 (현재 지도 기준)**
  - 윗줄 왼→오: **부스 4 · 3 · 2 · 1** (유치부A가 어린이실 쪽 끝)
  - 왼쪽 세로줄 위→아래: **부스 5 · 6 · 7**
  - 오른쪽 대각선 위→아래: **부스 12 · 13 · 10**
  - 하단 열: 본부(뽑기 교환처) / **부스14**(지도에는 번호 없이 베이지 `넥슨` 칸으로 표시, 클릭은 가능) / 미래엔(번호 없는 협력 부스, 클릭 불가) / **부스11**
  - 넥슨·미래엔 협력사 칸은 둘 다 **부스식 입체**(아래 4px 그림자 `#D8C7A6`) — 사용자 확정. 본부만 평면
- **표기 수정 이력**: "전동놀이"→**전통놀이**, 부스12 "지켜라"→**지게차**(구 프로토타입 오기), 부스 2 예약 게임은 **미니 우봉고**(Ubongo Mini, 이미지가 흐려 웹 검색 교차확인)
- **하단 안내문**: B1F 혜윰마당 대회장 안내 + 스탬프/뽑기 교환처 안내 포함

## 6. 남은 작업

- **부스 14(넥슨 슬라이딩 수학도둑) 설명 미입력** — 엑셀·전단 어디에도 없음. 현재 "체험 세부 내용은 준비되는 대로 안내됩니다" placeholder 노출 중

> **중요 원칙**: 확인 안 된 부스명·설명·대상을 **절대 임의로 만들어내지 말 것.**
> 자료로 확정되기 전까지는 placeholder를 유지합니다. (이 프로젝트에서 계속 지켜온 규칙)

## 7. 작업 시 주의사항

- ⚠️ **한글이 든 파일을 PowerShell `Get-Content -Raw` → `Set-Content`로 일괄 치환하지 말 것.**
  CP949 오독으로 파일 전체가 깨집니다(실제 사고 1회, `git checkout`으로 복원). 문자열 치환은 **Edit 도구**를 사용하세요.
- 자료 이미지는 `.gitignore`로 제외돼 있습니다. 공개 저장소라 원본 기획 자료를 올리지 않는다는 판단이며, 올리려면 사용자 확인을 먼저 받으세요.
- 수정 후 검증은 브라우저로 `index.html`을 열어 실제 렌더링 결과를 확인하는 흐름을 써 왔습니다(부스 개수, 좌표 순서, 시트 내용).

## 8. 향후 확장 아이디어 (사용자 언급)

- 예약 웹앱과 연결 시: `BOOTHS` 각 항목에 `reserveUrl` 필드 추가 + Bottom Sheet에 버튼 하나 추가하면 됨 (구조는 열어둔 상태)
- 부스별 QR은 딥링크로 이미 사용 가능

## 9. 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-09 | 최초 구축 — SVG 지도, 필터, Bottom Sheet, 핀치줌. 부스 4~7 명칭·전 부스 설명 미확정 상태로 배포 |
| 2026-08-13 | 갱신판 배치도 + 공식 홍보물 반영: 부스 6·7 명칭 확정, 전 부스 설명 입력, **위치 재배열**(윗줄 4·3·2·1 / 세로줄 7·5·6), 전통놀이 오타 수정, 본부 뽑기 교환처 표기, B1F 대회장 안내 추가 |
| 2026-08-13 | `정확한 부스명.xlsx` 기준으로 부스 2~7 명칭 최종 교체 |
| 2026-08-13 | 부스 8~14 대상 학년 제거 (원래 구분 없음) — 빈 `target`이면 UI에서 대상 줄 숨김 |
| 2026-08-13 | 예약/자유체험을 `reserve`·`free` 필드로 분리, 라벨 붙은 두 줄로 렌더링 |
| 2026-08-13 | 폴더 정리 — 원본 자료를 `자료/`로 이동, `.gitignore` 추가, 본 문서 재작성 |
| 2026-08-14 | 부스 14 협력사 표기를 미래엔과 통일 — 제목 `[넥슨재단]`→`[넥슨]`, 지도에서는 보라 번호 칸을 없애고 미래엔과 같은 베이지 `넥슨` 칸으로 교체 (`mapLabel` 필드 신설, 좌표는 미래엔과 동일한 `62×34`·같은 기준선 `y:355`·간격 8로 최종 확정) |
| 2026-08-14 | 전체 점검 — 협력사 칸 글자 광학 보정(`.map-label` weight 900: 획이 적은 `넥슨`이 `미래엔`보다 얇아 보이던 문제), 목록 칩 14번에 "지도에서는 '넥슨' 칸" 안내 추가, 지도 칸 `aria-label`에 협력사명 포함 |
| 2026-08-14 | 왼쪽 세로줄 순서를 위→아래 **7·5·6 → 5·6·7** 로 변경 (사용자 지시). `BOOTH_GEOM`의 y값만 교체 |
| 2026-08-14 | 지도 아래 「체험 예약하러 가기」 CTA 버튼 추가 — 예약 웹앱으로 연결, 예약코드 안내 문구 포함. 색은 **소프트 블루**(`#E9F2FB` / 테두리 `#CCE0F4` / 글자 `#1F6096`)로 확정. 진한 네이비(무겁다)·라벤더 그라데이션(튄다)·흰 카드(연하다)·라벤더 소프트(창의융합 색과 겹친다)를 거쳐 정착한 값입니다. **민트=보드게임, 라벤더=창의융합, 베이지=본부·협력로 이미 뜻이 정해져 있어 CTA에는 쓰지 말 것** |
